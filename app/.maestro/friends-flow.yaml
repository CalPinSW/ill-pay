appId: host.exp.Exponent
---
# Friends Management Flow E2E Test

- launchApp

# Wait for app to load
- extendedWaitUntil:
    visible: ".*"
    timeout: 15000

# Verify app is responsive
- assertVisible: ".*"
